[workspace]
resolver = "2"
members = ["drain3_rust", "codag-drain", "examples/server"]

# codag-drain workspace
#   drain3_rust  - vendored Rust Drain3 baseline
#   codag-drain  - deterministic log templating algorithm
#   examples/server - reference HTTP host for templating tests/service wiring
